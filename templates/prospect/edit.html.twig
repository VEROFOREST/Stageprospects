{% extends 'base.html.twig' %}

{% block title %}Edit Prospect{% endblock %}

{% block body %}
<div class="container">
    <h1 class="show">Edit Prospect</h1>

    {{ include('prospect/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('prospect_index') }}">back to list</a>

    {{ include('prospect/_delete_form.html.twig') }}
</div>
{% endblock %}
